// Importing the CSS for styling
import './Settings.css'

/**
 * Settings component that displays a message indicating the page is under construction.
 * It serves as a placeholder for future settings functionality.
 */
function Settings() {
  return (
    <>
      {/* Display the title of the settings page */}
      <h1>Réglages</h1>

      {/* Display a "coming soon" message */}
      <p>Coming Soon!</p>
    </>
  )
}

/**
 * Export the Settings component so it can be used in other parts of the application.
 * This allows it to be rendered when the corresponding route is accessed.
 */
export default Settings